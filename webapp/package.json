{
  "name": "ti-price-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "deploy": "npm run build && wrangler pages deploy dist --project-name ti-price-dashboard"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.6.0",
    "wrangler": "^3.78.0"
  }
}
